[project]
name = "grpo-playground"
version = "0.1.0"
description = "A learning project for implementing Transformer and GRPO from scratch"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "pyarrow>=25.0.1",
    "torch>=2.13.0",
]
